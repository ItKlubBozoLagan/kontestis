import {Snowflake} from "../lib/snowflake";

export const isAllowedToModifyContest = async (user: Snowflake, contest: Snowflake) => {
};

export const isAllowedToViewContest = async (user: Snowflake, contest: Snowflake) => {
}